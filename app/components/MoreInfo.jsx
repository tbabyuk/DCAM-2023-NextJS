

export const MoreInfo = ({info}) => {
    return ( 
        <div className="px-5 mt-10 lg:px-36 xl:px-64 text-center">
            <p className="italic text-gray-500">{info}</p>
        </div>
     );
}